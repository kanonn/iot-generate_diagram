お疲れ様です。ANEMSのFargate環境構築の進捗について共有いたします。

現在、リソースの申請は完了しておりますが、RNTBCI側の対応が一部未完了の状態です。
先方に問い合わせたところ、一部の設定（Task Definition等）はNDIチームの管轄であり、こちらへの権限付与もセキュリティ上不可との回答でした。

そのため、現在はNDIチームへ詳細を確認し、対応を依頼しているところです。進展があり次第、再度共有させていただきます。


{
    "family": "test-task",
    "containerDefinitions": [
        {
            "name": "test-container",
            "image": "alpine",
            "cpu": 256,
            "memory": 512,
            "essential": true
        }
    ],
    "requiresCompatibilities": ["FARGATE"],
    "networkMode": "awsvpc",
    "cpu": "256",
    "memory": "512"
}


aws ecs register-task-definition --cli-input-json file://test.json


"Hi Akhil and Sudharsan,"

"Regarding the deployment from EC2 as discussed, I have prepared the test.json and tried to register the Task Definition from the EC2 command line. However, the EC2's Host Role is being explicitly denied for both ecr:GetAuthorizationToken and ecs:RegisterTaskDefinition."

"Could you please coordinate to update the IAM policy for this EC2 instance? Without these permissions, I cannot follow the 'Mode 2' deployment process from the EC2 as suggested."
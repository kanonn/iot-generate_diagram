お疲れ様です。ANEMSのFargate環境構築の進捗について共有いたします。

現在、リソースの申請は完了しておりますが、RNTBCI側の対応が一部未完了の状態です。
先方に問い合わせたところ、一部の設定（Task Definition等）はNDIチームの管轄であり、こちらへの権限付与もセキュリティ上不可との回答でした。

そのため、現在はNDIチームへ詳細を確認し、対応を依頼しているところです。進展があり次第、再度共有させていただきます。


{
    "family": "test-task",
    "containerDefinitions": [
        {
            "name": "test-container",
            "image": "alpine",
            "cpu": 256,
            "memory": 512,
            "essential": true
        }
    ],
    "requiresCompatibilities": ["FARGATE"],
    "networkMode": "awsvpc",
    "cpu": "256",
    "memory": "512"
}

"Understood regarding the diagram. Let’s have a separate discussion on the deployment architecture later to align with NDI's requirements."

"In the meantime, could you please prioritize the other two technical blockers? They are essential for the ANEMS service to even start functioning:"

1. Connection Direction (SG):
The Fargate SG currently has port 23 in Inbound. Since Fargate is the client connecting to ANEMS, this must be an Outbound rule. Can we fix this to ensure the connection isn't blocked?

2. RDS Write Access:
We currently only have a Read-Only user. We cannot initialize the database schema or create tables for ANEMS with this. We need a user with Owner/Write permissions for the initial setup.

"Could you please confirm these two points? We need these resolved to make any progress on the connectivity test. Thanks!"

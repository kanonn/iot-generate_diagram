お疲れ様です。ANEMSのFargate環境構築の進捗について共有いたします。

現在、リソースの申請は完了しておりますが、RNTBCI側の対応が一部未完了の状態です。
先方に問い合わせたところ、一部の設定（Task Definition等）はNDIチームの管轄であり、こちらへの権限付与もセキュリティ上不可との回答でした。

そのため、現在はNDIチームへ詳細を確認し、対応を依頼しているところです。進展があり次第、再度共有させていただきます。


{
    "family": "test-task",
    "containerDefinitions": [
        {
            "name": "test-container",
            "image": "alpine",
            "cpu": 256,
            "memory": 512,
            "essential": true
        }
    ],
    "requiresCompatibilities": ["FARGATE"],
    "networkMode": "awsvpc",
    "cpu": "256",
    "memory": "512"
}

Gemini の回答
这一招非常老练，直接指向了**“资源初始化”**的真相。

如果对方拿到环境时这些东西就已经建好了，那就说明所谓的“架构图没写就不能建”纯属借口，因为系统可以根据一套默认标准自动生成这些基础组件。

你可以这样在 Teams 里发问：




Following up on our previous query, I would like to clarify that our current architecture does not utilize CI/CD. Therefore, we have not engaged the NDI team for any pipeline setup.

Our goal is to have the basic Task Definition, Task, and Service provisioned as part of the environment setup, identical to the TVSO team’s experience (which also didn't involve NDI according to their workflow).

Since these resources are currently missing from our environment:

Could you confirm if there is a way to trigger the creation of these core resources without a CI/CD pipeline?

What is the standard procedure for teams who don't require NDI/CI/CD but need the ECS resources pre-configured?

We look forward to your guidance on the next steps.


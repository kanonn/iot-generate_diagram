# 使用 sh -c 来执行复合命令
# 1. 后台启动 SpringBoot (&)
# 2. 睡眠 30 秒等待应用完全启动 (sleep 30)
# 3. 执行 curl 并把结果打印到控制台 (即日志)
# 4. 使用 wait 命令防止容器在 curl 结束后退出
CMD sh -c "java -jar /app.jar & sleep 30; echo '>>> Starting Self-Test Curl...'; curl -v http://localhost:8080/你的API路径; wait"

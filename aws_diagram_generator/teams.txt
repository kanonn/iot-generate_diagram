aws ecs run-task \
    --cluster [你的集群名] \
    --task-definition [你刚建好的那个Family名] \
    --launch-type FARGATE \
    --network-configuration '{
        "awsvpcConfiguration": {
            "subnets": ["你的子网ID"],
            "securityGroups": ["你的安全组ID"],
            "assignPublicIp": "DISABLED"
        }
    }'

"Hi Sudharsan, I'm trying to start the dproto-qa-ec2 instance, but it failed with a KMS encryption error."

"It seems the service lacks permissions for the KMS key used by the EBS volume (e.g., kms:Decrypt, kms:CreateGrant). Could you please fix the KMS key policy or IAM permissions?"

"Also, I noticed port 22 is not open in the Security Group. Should I use SSM Session Manager to connect once the instance is running?"
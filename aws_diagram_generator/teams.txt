お疲れ様です。ANEMSのFargate環境構築の進捗について共有いたします。

現在、リソースの申請は完了しておりますが、RNTBCI側の対応が一部未完了の状態です。
先方に問い合わせたところ、一部の設定（Task Definition等）はNDIチームの管轄であり、こちらへの権限付与もセキュリティ上不可との回答でした。

そのため、現在はNDIチームへ詳細を確認し、対応を依頼しているところです。進展があり次第、再度共有させていただきます。


{
    "family": "test-task",
    "containerDefinitions": [
        {
            "name": "test-container",
            "image": "alpine",
            "cpu": 256,
            "memory": 512,
            "essential": true
        }
    ],
    "requiresCompatibilities": ["FARGATE"],
    "networkMode": "awsvpc",
    "cpu": "256",
    "memory": "512"
}


aws ecs register-task-definition --cli-input-json file://test.json


"NDI team (Akhil) confirmed we should deploy from this EC2, but I'm currently hit by an Explicit Deny on ECR/ECS actions. 
It’s a major blocker for the ANEMS deployment. Could you please take a quick look at the email and screenshots when you're back? Thanks!"




Hi Sudharsan, sorry to add more, but I found two other potential blockers that need your confirmation:

1. Security Group (Fargate -> ANEMS):
I noticed port 23 (for ANEMS) was added to the Inbound rules of the Fargate SG. Since our Fargate task acts as the client initiating the connection to ANEMS, shouldn't this be an Outbound rule instead? Please double-check if the current setup will block our traffic.

2. RDS (PostgreSQL) Permissions:
I noticed we only have a Read-Only user for the PostgreSQL RDS. We need to create tables and perform initial DB setups for the ANEMS service. How should we handle the DDL/DML operations? Should we be granted a user with higher privileges (e.g., rds_superuser or a dedicated owner role)?

Could you please clarify these points? It would save us a lot of troubleshooting time later. Thanks!